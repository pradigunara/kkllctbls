import {
    castArray, chunk, cloneDeep, constant, entries, filter, find, get, identity, kebabCase, keys, last, map, noop, pick, set, sortBy, includes
} from 'lodash-es'

const lodash = {
    castArray, chunk, cloneDeep, constant, entries, filter, find, get, identity, kebabCase, keys, last, map, noop, pick, set, sortBy, includes
}

export default lodash