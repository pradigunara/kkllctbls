import {
    castArray, chunk, cloneDeep, constant, entries, filter, find, get, identity, kebabCase, keys, last, map, noop, pick, set, sortBy
} from 'lodash-es'

const lodash = {
    castArray, chunk, cloneDeep, constant, entries, filter, find, get, identity, kebabCase, keys, last, map, noop, pick, set, sortBy
}

export default lodash